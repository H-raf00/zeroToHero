info:
  name: testBasicApi
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5050/api/user/create
  body:
    type: json
    data: |-
      {
        "FirstName": "Marlon",
        "LastName": "Brando",
        "Age": 40
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
